<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. Using Material3 -->
    <style name="Base.Theme.TuMatchApp_prueba" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Color mapping -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorOnPrimary">@color/onPrimary</item>
        <item name="colorPrimaryContainer">@color/primaryContainer</item>

        <item name="colorSecondary">@color/secondary</item>
        <item name="colorOnSecondary">@color/onSecondary</item>

        <item name="android:colorBackground">@color/background</item>
        <item name="colorSurface">@color/surface</item>
        <item name="colorOnBackground">@color/onBackground</item>
        <item name="colorOnSurface">@color/onSurface</item>

        <item name="colorError">@color/error</item>

        <!-- Customize Material components appearance -->
        <item name="materialButtonStyle">?attr/materialButtonStyle</item>
    </style>

    <style name="Theme.TuMatchApp_prueba" parent="Base.Theme.TuMatchApp_prueba" />
</resources>