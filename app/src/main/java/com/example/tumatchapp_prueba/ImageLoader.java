package com.example.tumatchapp_prueba;

// ImageLoader quedado obsoleto: ahora usamos Glide para carga y cache de imágenes.
// Conservado sólo como marcador; eliminar si se prefiere.

public class ImageLoader {
    private ImageLoader() { /* no-op */ }
}
